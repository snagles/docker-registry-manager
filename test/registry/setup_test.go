package registry

/*
func TestSetup(t *testing.T) {
	Start("./config-tls-no-auth.yml")
		if err := Seed("localhost:5000"); err != nil {
			t.Fatalf("Unable to seed repository: %s", err.Error())
		}
	Stop()
}
*/
